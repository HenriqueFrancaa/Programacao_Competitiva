#include <iostream>
#include <vector>
#include <string>
using namespace std;

int main(){
    int t,a,b;
    string s;
    cin >> s;
    cin >> t;
    vector<int>A;
    vector<int>B;
    for(int i = 0; i < t; i++){
        cin >> a >> b;
        A.push_back(a);
        B.push_back(b);
    }

}
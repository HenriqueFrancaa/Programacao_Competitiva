#include <stdio.h>

int main(){

    int L,H;
    int l1,h1,l2,h2;

    scanf("%d %d",&L, &H);
    scanf("%d %d %d %d", &l1, &h1, &l2, &h2);

    int area_pagina = L * H;

    int area_ft1 = l1 * h1;
    int area_ft2 = l2 * h2;

    int area_total = area_ft1 + area_ft2;

    int altura_fotos = h1 + h2;
    int largura_fotos = l1 + l2;

    if()

    return 0;

}